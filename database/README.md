# This directory is managed by the ETL pipeline.
# The SQLite database file `sales.db` will be created here automatically
# when the `load_to_sqlite` Airflow task runs for the first time.
#
# Do NOT delete this directory — it must exist for the Docker volume mount
# in docker-compose.yaml to work correctly.
